import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
  btn: {
    paddingVertical: 14,
    alignItems: 'center',
    borderRadius: 4,
  },
  label: {
    fontWeight: 'bold',
  },
});

export default styles;
